[package]
name = "netcli_core"
version = "0.1.0"
edition = "2021"
description = "Network CLI output parsing SDK — platform-aware template dispatch"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
