[package]
name = "netcli_ffi"
version = "0.1.0"
edition = "2021"
description = "C ABI wrapper for netcli_core — exposes netcli_parse_json / netcli_free"

[lib]
crate-type = ["cdylib", "staticlib"]

[dependencies]
netcli_core = { path = "../netcli_core" }

[dev-dependencies]
serde_json = "1"
